# План рефакторинга: убираем подзадачи

## ✅ Выполнено

1. **Task.java** - обновлена модель:
   - Удалены: `parentTask`, `subtasks`, `subtaskType`, `SubtaskType enum`
   - Добавлены поля для разделов:
     - GIS: `gisAssessmentDate`, `gisComment`, `gisStatus`
     - EGRN: `egrnRequestDate`, `egrnStatus`
     - FIAS: `fiasCheckDate`, `fiasStatus`
     - Статусы: `documentUploadStatus`, `paymentStatus`
   - Добавлен: `TaskStepStatus enum` (NOT_STARTED, IN_PROGRESS, COMPLETED, SKIPPED)
   - Добавлены геттеры/сеттеры

## 🔄 В процессе

2. **TaskRequest.java** - DTO для запросов:
   - Удалить: `subtaskType`
   - Добавить поля для всех разделов

3. **TaskResponse.java** - DTO для ответов:
   - Удалить: `parentTaskId`, `subtaskType`
   - Добавить поля для всех разделов

4. **TaskService.java** - бизнес-логика:
   - Удалить методы работы с подзадачами: `createSubtask`, `getSubtasks`
   - Обновить `createTask`, `updateTask` для работы с новыми полями
   - Удалить ссылки на `subtaskType`

5. **TaskController.java** - API endpoints:
   - Удалить эндпоинты: `GET /{id}/subtasks`, `POST /{id}/subtasks`

6. **TaskRepository.java** - проверить методы

7. **Frontend types** - обновить TypeScript типы

8. **Frontend components** - обновить компоненты:
   - `EditProcessTask.tsx` - переделать логику сохранения
   - Убрать цикл по подзадачам
   - Сохранять все данные в одной задаче

## 📋 Что нужно сделать

### Backend:
- [ ] Обновить TaskRequest.java
- [ ] Обновить TaskResponse.java
- [ ] Обновить TaskService.java
- [ ] Обновить TaskController.java
- [ ] Миграция БД (удалить parent_task_id, subtask_type)

### Frontend:
- [ ] Обновить types/index.ts (убрать SubtaskType, добавить новые поля)
- [ ] Обновить EditProcessTask.tsx (убрать загрузку подзадач, изменить сохранение)
- [ ] Обновить форму (сохранять не подзадачи, а разделы одной задачи)
- [ ] Убрать список "Мои задачи" на главной вкладке (это были подзадачи)

## Преимущества нового подхода

1. **Проще**: Одна сущность вместо множества связанных
2. **Быстрее**: Меньше запросов к БД
3. **Понятнее**: Вся информация о задаче в одном месте
4. **Атомарность**: Сохранение всех данных за одну операцию
